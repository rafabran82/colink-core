param(
    [int]$TopN = 20,
    [switch]$TestMode
)

Write-Host "✔ Minimal LP Simulation running..." -ForegroundColor Green
Write-Host "TopN = $TopN" -ForegroundColor Cyan
Write-Host "TestMode = $TestMode" -ForegroundColor Cyan

return 0
