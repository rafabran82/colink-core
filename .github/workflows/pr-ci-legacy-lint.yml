name: PR CI (legacy lint shim)
on:
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  lint:
    name: PR CI / lint
    runs-on: ubuntu-latest
    steps:
      - run: echo "Satisfy legacy PR CI / lint required check"
