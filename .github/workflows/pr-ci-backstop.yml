name: PR CI
on:
  pull_request:
permissions:
  contents: read
jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - name: Say hi (backstop)
        run: echo "Backstop PR CI running to satisfy required context."
