name: AWS Terraform Plan

on:
  workflow_dispatch:

permissions:
  id-token: write     # OIDC → AWS
  contents: read

jobs:
  plan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      # (keep your AWS creds / terraform steps here)
