name: Hello Probe

on:
  push:
    branches: [ chore/phase3-metrics-schema-and-collector ]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from ${{ github.ref }} @ ${{ github.sha }}"