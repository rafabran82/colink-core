import React from "react";

// SAFE FlashGraph placeholder
export default function FlashGraph() {
  return (
    <div
      style={{
        width: "100%",
        height: "280px",
        borderRadius: "18px",
        background: "linear-gradient(135deg, #222, #333)",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        color: "#fff",
        fontSize: "22px",
        fontWeight: "bold",
        animation: "pulse 2s infinite",
      }}
    >
       FlashGraph Placeholder (No Data Required)
    </div>
  );
}

